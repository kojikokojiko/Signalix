FROM golang:1.22-alpine

WORKDIR /app

# Air (ホットリロードツール) をインストール
RUN go install github.com/air-verse/air@v1.52.3

# 依存関係を先にキャッシュ
COPY go.mod ./
RUN go mod download

# ソースはボリュームマウントで渡すので COPY 不要
CMD ["air", "-c", ".air.toml"]
